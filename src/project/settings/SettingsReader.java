package src.project.settings;

import src.project.file.WikiFile;

public class SettingsReader {

	// Search for settings (XhtmlSettings)
	public void searchAndReplace(WikiFile file) {
		StringBuffer in = file.getContent();
	}
	
}
